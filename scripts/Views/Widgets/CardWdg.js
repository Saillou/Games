/*
	Card object - Display card - view
*/

var CardWdg = function (ctx_, cardModel_) {
    Widget.call(this, ctx_);

    // Public members
    this.cardModel = cardModel_;
    this.hidden = false;

    // Public methods

};