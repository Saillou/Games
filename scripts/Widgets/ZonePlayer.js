/*

*/
var ZonePlayer = function (ctx_, playerModel_) {
    Widget.call(this, ctx_);

    // Public members
    this.playerModel = playerModel_;

    /*
        infos.draw();
        handCards.draw();
            for(card : cards) card.draw();
        lastCard.draw();
        status.draw();
            for(card : immunityCard) card.draw()
    */
};