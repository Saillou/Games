/*

*/
var ZoneCommon = function (ctx_, game_) {
    Widget.call(this, ctx_);

    // Public members
    this.game = game_;

    // Public methods
    /* 
        var modelCardPioche = this.game.cardsRemaining.length > 0 ? this.game.cardsRemaining[0] : null;
        var modelCardThowed = this.game.cardsThrowed.length > 0 ? this.game.cardsThrowed[0] : null;

        modelCardPioche.visible = false;
        modelCardThowed.visible = true;

        ..
    */

};